#include <stdio.h>
// C언어에서는 엔터/띄어쓰기 등에 대해서, 굉장히 관대하다.
// 작성 스타일(Style)만 있고, 그 외엔 없음
// 단, 명령어/절차를 작성할 때는, 구분할 것은 명확하게 구분해야 된다.
// 또한, 마침표를 넣어줘야 하고, 이를 세미콜론이 대체한다.
int main(void) {
	// 아버지가 방에 들어 가신다.
	return 0;
}
// 코드의 실행은 왼쪽 ctrl키 누른 상태로 F5를 누른다.
// - 소스파일이 여러개 있으면 각 소스파일에 적절하게 코드를
//   분할시켜서 프로그램을 만들게 된다.
// - 소스파일별로 중첩되는 내용이 있으면 실행이 안된다.
//   필요없는 파일은 삭제/목록에서 제외/실행할 때는 제외 중 하나를 선택
